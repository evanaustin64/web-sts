// app/maintenance/layout.tsx

export default function MaintenanceLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  // Cukup kembalikan children. Boleh menggunakan React Fragment (<>) 
  // atau biarkan seperti ini saja.
  return <>{children}</>;
}